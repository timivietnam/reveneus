fragment Member on Member {
  id
  avatar_url
  full_name
  hour_rate
  role
  is_active
  user_id
  ProjectMembers {
    user_id
    updated_at
    project_id
    member_id
    hour_rate
    created_at
    id
    hour_working
  }
}
